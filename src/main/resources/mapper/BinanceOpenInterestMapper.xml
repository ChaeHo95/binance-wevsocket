<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.binancewebsocket.mapper.BinanceOpenInterestMapper">

    <insert id="insertOpenInterest" parameterType="com.example.binancewebsocket.dto.BinanceOpenInterestDto">
        INSERT INTO binance_open_interest (symbol, open_interest, time_stamp)
        VALUES (#{symbol}, #{openInterest}, #{time}) ON DUPLICATE KEY
        UPDATE
            open_interest = IF(VALUES (open_interest) != open_interest, VALUES (open_interest), open_interest);
    </insert>
</mapper>
